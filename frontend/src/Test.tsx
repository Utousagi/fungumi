import { Button } from "@arco-design/web-react";
// import { getSubjectListByRank } from "./axios/Search";

export default function () {
  return (
    <Button
      onClick={() => {
        // var result = getSubjectListByRank(10,1)
        // console.log(result)
      }}
    >
      TEST
    </Button>
  );
}
